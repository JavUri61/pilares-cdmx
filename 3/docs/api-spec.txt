
**`docs/api-spec.yaml`**
```yaml
openapi: 3.0.0
info:
  title: PILARES CDMX API
  version: 1.0.0
paths:
  /sedes:
    get:
      summary: Get all locations
      responses:
        '200':
          description: List of locations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Sede'
components:
  schemas:
    Sede:
      type: object
      properties:
        id:
          type: integer
        nombre:
          type: string
        direccion:
          type: string